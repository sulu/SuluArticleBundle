sulu_article: {}

sulu_core:
    content:
        structure:
            default_type:
                article: 'article'

framework:
    # symfony recipe default configuration:
    workflows: null
    messenger:
        transports:
        routing:

twig:
    paths:
        - '%kernel.project_dir%/templates'
